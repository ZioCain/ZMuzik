import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ziocain.zmuzik',
  appName: 'ZMuzik',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },

};

export default config;
